services:
  web:
    container_name: websitebama
    build:
      context: .
      args:
        NODE_IMAGE: node:20-alpine
    ports:
      - "3022:3022"
    environment:
      PORT: ${PORT:-3022}
    restart: unless-stopped
